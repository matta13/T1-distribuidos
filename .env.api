# === Postgres (tu contenedor: postgres_db) ===
POSTGRES_HOST=postgres
POSTGRES_DB=mydatabase
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123

# === Redis (tu contenedor: redis_cache) ===
REDIS_HOST=redis_cache
REDIS_PORT=6379

# === Ollama (tu contenedor: ollama) ===
OLLAMA_HOST=http://ollama:11434
OLLAMA_MODEL=llama3

# === Cache TTL (7 días)
CACHE_TTL_SECONDS=604800
